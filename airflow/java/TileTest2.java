package egovframework.innopam.tile;

import org.junit.jupiter.api.Test;

public class TileTest2 {
//    @Test
//    public void test(){
//        File tif = new File("path_to_your_tif.tif");
//        AbstractGridFormat format = new GeoTiffFormat();
//
//        Hint hint = new Hints(Hints.FORCE_LONGITUDE_FIRST_AXIS_ORDER, Boolean.TRUE);
//        GridCoverage2DReader reader = format.getReader(tif, hint);
//
//        GridEnvelope dimensions = reader.getOriginalGridRange();
//        GeneralEnvelope envelope = reader.getOriginalEnvelope();
//
//        double minX = envelope.getMinimum(0);
//        double maxX = envelope.getMaximum(0);
//        double minY = envelope.getMinimum(1);
//        double maxY = envelope.getMaximum(1);
//
//        n = 2.0 ^ z
//        lon_deg = x / n * 360.0 - 180.0
//        lat_rad = atan(sinh(π * (1 - 2 * y / n)))
//        lat_deg = lat_rad * (180 / π)
//
//        double n = Math.pow(2.0, z);
//        double lonDeg = x / n * 360.0 - 180.0;
//        double latRad = Math.atan(Math.sinh(Math.PI * (1 - 2 * y / n)));
//        double latDeg = Math.toDegrees(latRad);
//    }

}
